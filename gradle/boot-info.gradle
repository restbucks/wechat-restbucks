gitProperties {
    dateFormat = "yyyy-MM-dd'T'HH:mmZ"
}

springBoot {
    buildInfo {
        additionalProperties = [
                'group'  : "$project.group",
                'version': "$gitVersionName",
        ]
    }
}